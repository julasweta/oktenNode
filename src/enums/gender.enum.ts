export enum EGenders {
  Male = "male",
  Female = "female",
  Other = "other",
}

export enum EUserStatus {
  active = "active",
  inactive = "inactive",
  deleted = "deleted",
}

export enum EActionTokenType {
  activate = "activate",
  forgotPassword = "forgot",
}
